#set page(width: auto, height: auto, margin: 8pt)
#set text(size: 10pt)
#import "/blockcell/lib.typ": *

#align(center)[*Alt / else fragment*]

#seq-puml(width: 360pt, "participant Browser
participant API

Browser -> API : GET /resource
alt cache hit
  API --> Browser : 200 cached
else cache miss
  API --> Browser : 200 fresh
end
")
