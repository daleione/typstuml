#set page(width: auto, height: auto, margin: 8pt)
#set text(size: 10pt)
#import "/blockcell/lib.typ": *

#align(center)[*Self-message regression*]

#seq-puml(width: 360pt, "participant Cache

Cache -> Cache : evict expired
Cache -> Cache : compact entries
")
