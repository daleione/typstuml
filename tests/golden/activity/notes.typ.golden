#set page(width: auto, height: auto, margin: 8pt)
#set text(size: 10pt)
#import "/blockcell/lib.typ": *

#align(center, flow-col(
  start-marker(),
  with-notes(
  process[Receive request],
  right-notes: (flow-note([arrives via HTTP]), ),
),
  with-notes(
  process[Validate input],
  left-notes: (flow-note([  rejects on \   schema error]), ),
),
  with-notes(
  process[Compute response],
  right-notes: (flow-note([  cache hit \   not measured]), ),
),
  stop-marker(),
))
