#set page(width: auto, height: auto, margin: 8pt)
#set text(size: 10pt)
#import "/blockcell/lib.typ": *

#align(center)[*Build → test → deploy*]

#align(center, flow-col(
  start-marker(),
  process[Checkout sources],
  process[Compile],
  process[Unit tests],
  process[Integration tests],
  process[Package artifact],
  process[Promote to staging],
  process[Smoke test],
  process[Promote to production],
  stop-marker(),
))
